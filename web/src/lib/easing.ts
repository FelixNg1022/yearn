export type E4 = [number, number, number, number]

/** Exponential out — fast entry, smooth landing. */
export const EXPO: E4 = [0.16, 1, 0.3, 1]

/** Smooth in-out — gentle sway and idle loops. */
export const SOFT: E4 = [0.65, 0, 0.35, 1]
